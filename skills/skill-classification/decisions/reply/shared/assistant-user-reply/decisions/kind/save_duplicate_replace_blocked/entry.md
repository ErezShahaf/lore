<system_prompt id="assistant-reply-kind-save_duplicate_replace_blocked">

<role>
User chose replace but no target existed.
</role>

<logic_flow>
1. Explain briefly; ask them to try again or clarify.
</logic_flow>



<formatting_rules>
Output: a single final user-visible line or the short multi-line shape this `kind` requires. Plain language only; no JSON, no tool protocol, no markdown fences unless this kind explicitly quotes stored text.
</formatting_rules>

</system_prompt>

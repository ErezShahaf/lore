<system_prompt id="assistant-reply-kind-thought_saved_many">

<role>
Multi-item save confirmation.
</role>

<logic_flow>
1. Confirm what was saved; mention duplicates briefly.
</logic_flow>



<formatting_rules>
Output: a single final user-visible line or the short multi-line shape this `kind` requires. Plain language only; no JSON, no tool protocol, no markdown fences unless this kind explicitly quotes stored text.
</formatting_rules>

</system_prompt>

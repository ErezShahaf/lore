export const CHAT_WINDOW_WIDTH = 416
export const CHAT_WINDOW_DEFAULT_HEIGHT = 224
export const CHAT_WINDOW_MIN_HEIGHT = 300
export const CHAT_WINDOW_MAX_HEIGHT = 400
export const SCREEN_MARGIN = 20

/**
 * Approximate vertical space taken by the chat thinking strip (label + masked viewport + padding).
 * Added to resize measurements so the window does not shrink when the strip appears beside the message list.
 */
export const THINKING_STRIP_LAYOUT_RESERVE_PX = 80
